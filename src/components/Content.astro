---
import { AstroComponentInstance } from 'astro/dist/runtime/server';

export interface Props {
    className?: string
}
const {className ='' } = Astro.props
---

<article class={`px-8 mx-auto max-w ${className}`}><slot /></article>